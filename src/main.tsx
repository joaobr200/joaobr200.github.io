import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "swiper/css";

const app = document.getElementById("root");

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  app
);
